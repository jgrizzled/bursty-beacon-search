[project]
name = "bursty-beacon-search"
version = "0.1.0"
description = "H1 repeater scanner-model timing analysis (prereg_h1) and archival search tooling"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.5,<3",
    "scipy>=1.18,<2",
    "astropy>=8.0,<9",
]

[tool.uv]
package = false
